rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access uses authenticated server APIs and Firebase Admin.
    // Keep posts, prediction lifecycle and privacy checks behind those APIs.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
