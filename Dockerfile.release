FROM node:20-bookworm-slim
WORKDIR /app
ENV NODE_ENV=production
ENV PORT=8080
ENV HOSTNAME=0.0.0.0
# Already compiled and checked on the Linux release runner.
COPY app ./
EXPOSE 8080
CMD ["node", "server.js"]
