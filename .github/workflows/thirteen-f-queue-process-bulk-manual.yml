name: Process 13F Queue Bulk

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Target environment
        required: true
        default: staging
        type: choice
        options:
          - staging
          - production
      batch_size:
        description: Max queued filings to process per API call
        required: true
        default: "100"
        type: string
      max_batches:
        description: Max API calls to make in this workflow run
        required: true
        default: "150"
        type: string
      target_total:
        description: Stop after this many filings have been processed
        required: true
        default: "15000"
        type: string
      sleep_seconds:
        description: Seconds to wait between API calls
        required: true
        default: "2"
        type: string
      dry_run:
        description: Parse without queue status updates or holding writes
        required: true
        default: false
        type: boolean
      include_stale_processing:
        description: Retry stale PROCESSING filings
        required: true
        default: true
        type: boolean
      stale_processing_minutes:
        description: PROCESSING age before a filing is considered stale
        required: true
        default: "60"
        type: string

permissions:
  contents: read

concurrency:
  group: thirteen-f-queue-process-bulk-${{ inputs.environment }}
  cancel-in-progress: false

jobs:
  process-13f-queue-bulk:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    environment: ${{ inputs.environment }}
    env:
      TARGET_ENVIRONMENT: ${{ inputs.environment }}
      GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID }}
      GCP_REGION: ${{ vars.GCP_REGION }}
      CLOUD_RUN_SERVICE_STAGING: ${{ vars.CLOUD_RUN_SERVICE_STAGING }}
      CLOUD_RUN_SERVICE_PRODUCTION: ${{ vars.CLOUD_RUN_SERVICE_PRODUCTION }}
      INTERNAL_API_TOKEN: ${{ secrets.INTERNAL_API_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}

      - name: Resolve Cloud Run URL
        id: cloud-run
        run: |
          set -euo pipefail
          service="$CLOUD_RUN_SERVICE_PRODUCTION"
          if [[ "$TARGET_ENVIRONMENT" == "staging" ]]; then
            service="$CLOUD_RUN_SERVICE_STAGING"
          fi

          if [[ -z "$service" ]]; then
            echo "Cloud Run service name is not configured for $TARGET_ENVIRONMENT" >&2
            exit 1
          fi

          url="$(gcloud run services describe "$service" \
            --project "$GCP_PROJECT_ID" \
            --region "$GCP_REGION" \
            --format='value(status.url)')"

          if [[ -z "$url" ]]; then
            echo "Unable to resolve Cloud Run URL for $service" >&2
            exit 1
          fi

          echo "url=$url" >> "$GITHUB_OUTPUT"

      - name: Process 13F queue in batches
        env:
          CLOUD_RUN_URL: ${{ steps.cloud-run.outputs.url }}
          BATCH_SIZE: ${{ inputs.batch_size }}
          MAX_BATCHES: ${{ inputs.max_batches }}
          TARGET_TOTAL: ${{ inputs.target_total }}
          SLEEP_SECONDS: ${{ inputs.sleep_seconds }}
          DRY_RUN: ${{ inputs.dry_run }}
          INCLUDE_STALE_PROCESSING: ${{ inputs.include_stale_processing }}
          STALE_PROCESSING_MINUTES: ${{ inputs.stale_processing_minutes }}
        run: |
          set -euo pipefail
          if [[ -z "$INTERNAL_API_TOKEN" ]]; then
            echo "INTERNAL_API_TOKEN secret is not configured" >&2
            exit 1
          fi

          total_processed=0
          total_parsed=0
          total_failed=0
          total_skipped=0

          for ((batch=1; batch<=MAX_BATCHES; batch++)); do
            body="$(jq -n \
              --argjson limit "$BATCH_SIZE" \
              --argjson dryRun "$DRY_RUN" \
              --argjson includeStaleProcessing "$INCLUDE_STALE_PROCESSING" \
              --argjson staleProcessingMinutes "$STALE_PROCESSING_MINUTES" \
              '{
                limit: $limit,
                dryRun: $dryRun,
                includeStaleProcessing: $includeStaleProcessing,
                staleProcessingMinutes: $staleProcessingMinutes
              }')"

            echo "Processing 13F queue batch ${batch}/${MAX_BATCHES}"
            response_file="$(mktemp)"
            status="$(curl --silent --show-error \
              --output "$response_file" \
              --write-out '%{http_code}' \
              --request POST \
              --header "Authorization: Bearer ${INTERNAL_API_TOKEN}" \
              --header "Content-Type: application/json" \
              --data "$body" \
              "${CLOUD_RUN_URL}/api/internal/securities/process-13f-queue" || true)"

            cat "$response_file"
            echo

            if [[ "$status" -lt 200 || "$status" -ge 300 ]]; then
              echo "13F queue processing failed with HTTP $status on batch ${batch}" >&2
              exit 1
            fi

            candidates="$(jq -r '.candidatesFound // 0' "$response_file")"
            processed="$(jq -r '.processed // 0' "$response_file")"
            parsed="$(jq -r '.parsed // 0' "$response_file")"
            failed="$(jq -r '.failed // 0' "$response_file")"
            skipped="$(jq -r '.skipped // 0' "$response_file")"

            total_processed=$((total_processed + processed))
            total_parsed=$((total_parsed + parsed))
            total_failed=$((total_failed + failed))
            total_skipped=$((total_skipped + skipped))

            echo "Batch ${batch}: candidates=${candidates}, processed=${processed}, parsed=${parsed}, failed=${failed}, skipped=${skipped}"
            echo "Totals: processed=${total_processed}, parsed=${total_parsed}, failed=${total_failed}, skipped=${total_skipped}"

            if [[ "$DRY_RUN" == "true" ]]; then
              echo "Dry run does not claim queue documents, so stopping after one batch."
              break
            fi

            if [[ "$candidates" -eq 0 ]]; then
              echo "No more queued 13F filings found. Stopping."
              break
            fi

            if [[ "$total_processed" -ge "$TARGET_TOTAL" ]]; then
              echo "Target total ${TARGET_TOTAL} reached. Stopping."
              break
            fi

            sleep "$SLEEP_SECONDS"
          done
